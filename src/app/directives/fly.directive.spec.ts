import { FlyDirective } from './fly.directive';

describe('FlyDirective', () => {
  it('should create an instance', () => {
    const directive = new FlyDirective();
    expect(directive).toBeTruthy();
  });
});
